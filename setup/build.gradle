if (project.hasProperty("topobyte")) {
    apply from: 'file:///' + System.getProperty("user.home") \
        + '/.gradle/topobyte.gradle'
}

dependencies {
    api project(':nomioc-processing')
}

publishing {
    publications {
        maven(MavenPublication) {
            from components.java
        }
    }
}
